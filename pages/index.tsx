import type { NextPage } from "next";
import { BaseLayout } from "../src/layout/base-layout";

const Home: NextPage = () => {
  return <BaseLayout>Layout was done</BaseLayout>;
};

export default Home;
